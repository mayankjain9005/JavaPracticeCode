# Starter pipeline
# Start with a minimal pipeline that you can customize to build and deploy your code.
# Add steps that build, run tests, deploy, and more:
# https://aka.ms/yaml

trigger:
- master

pool:
  name: default

steps:
- script: echo Hello, world!
  displayName: 'Run a one-line script'

- script: |
    echo Add other tasks to build, test, and deploy your project.
    echo See https://aka.ms/yaml
  displayName: 'Run a multi-line script'

- task: azure-deveops-ispw-operation-task@1
  inputs:
    connectionId: 'cw09.compuware.com:47624#1047 - Latin 1'
    cesUrl: 'http://localhost:48080'
    cesSecretToken: '1fa526c3-6be5-4181-a4ff-10abb4c2185a	'
    action: 'PromoteAssignment'
    request: |
      assignmentId=HARY008369
       level= STG1
    skipWaitingForSetCompletion: 'No'
    showResponseBodyInConsole: 'No'